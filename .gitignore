# GENERATED by scripts/deploy.py from ALLOWED and ALLOWED_PREFIXES.
# Edit those, not this file.
# Deny everything, then allow by name — tooling in the parent directory has
# written stray state into this checkout, and a 1.5 MB database once reached
# a public repo as a result.
*
!.gitignore
!.nojekyll
!README.md
!icon.svg
!index.html
!manifest.webmanifest
!search.json
!sw.js
!days/
!days/*.json
!text-jps/
!text-jps/*.json
!text/
!text/*.json
!themes/
!themes/*.json
